
function setInterval(window, value) {
    window.setInterval=(value)=>{}
}(window||global)

function formatDate(date, format) {

}

// export const DateSystem = new Date();
// export const MONTHS = [31,28,31,30,31,];

/* 
let x = (function foo(a){
    setInterval=()=>{	
    }(window||global))

function addSecond(date, value) {
    myVar = setInterval(alertFunc, 3000);
}(window||global)


function renderDay(Day) {

    html`<span>day.day</span>`
} 

function* range(to, from) {
    for (var i=to; i<from; i++) {
        yield i;
    }
}

[...range(1, 31)].map()

*/

